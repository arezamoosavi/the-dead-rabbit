version: "3"

services:
  rabbit:
    build:
      context: .
      dockerfile: Dockerfile
#    volumes:
#      - ./:/opt/project
#    ports:
#      - 8080:8080
    restart: unless-stopped
#    command: sleep infinity
#    command: java -cp /opt/project/target/the-dead-rabbit-1.0-SNAPSHOT.jar org.bigrabbit.com.PingRabbit

